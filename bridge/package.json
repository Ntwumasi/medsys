{
  "name": "medsys-bridge",
  "version": "0.1.0",
  "description": "On-prem bridge between MedSys cloud and Orthanc PACS",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node --transpile-only src/index.ts",
    "dev": "nodemon --watch src --ext ts --exec 'ts-node --transpile-only' src/index.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.9.0"
  }
}
